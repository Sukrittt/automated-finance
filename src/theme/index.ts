import { colors, spacing, radius, typography, shadows, motion } from './tokens';

export const theme = {
  colors,
  spacing,
  radius,
  typography,
  shadows,
  motion
};

export type Theme = typeof theme;
